import React from "react";
import { Gri, Typography } from "@mui/material";

const Movie = () => {
    return (
        <Grid container={true} spacing={2}>
            <Grid item={true} md={3}></Grid>
                {
                    movie.poster_path
                }
            <Grid item={true} md={9}></Grid>
        </Grid>
    );

};

export default Movie;